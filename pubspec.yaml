name: flutter_micro_frontends
description: Paquete para Dart que proporciona una arquitectura de micro frontends para la construcción y despliegue de aplicaciones de frontend escalables y mantenibles
version: 0.0.1
homepage: 

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
